# Database Configuration (SQLite)
DB_PATH=./database/store_rating.db
# Set to true to use mock database instead of SQLite
USE_MOCK_DB=false

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_for_store_rating_platform_2024
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:3000
